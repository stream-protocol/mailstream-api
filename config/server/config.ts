export const dbConfig = {
    host: 'localhost',
    port: 5432,
    username: 'dbUser',
    password: 'dbPassword',
    database: 'dbName',
  };
  export const serverPort = 8080;
  // Other server-specific settings
  